package de.bellmannjan.javaguibuilder.Components;

import javax.swing.*;

public class ResizableButton extends ResizableComponent {
    public ResizableButton(JComponent comp, boolean hasScrollPane) {
        super(comp, hasScrollPane);
    }
}
